api:
  secrets:
    appsettings.secure.json:
      data: {}

queue-consumers:
  secrets:
    data:
      OlsProcessorQueue:
        username: '{{ vault "engineering/ols/olsprocessor/${DEPLOYMENT}/processorRabbitMq" "username" }}'
        password: '{{ vault "engineering/ols/olsprocessor/${DEPLOYMENT}/processorRabbitMq" "password" }}'
